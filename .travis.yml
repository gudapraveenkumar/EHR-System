language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - echo "TRAVIS_BRANCH=$TRAVIS_BRANCH, PULL_REQUEST_BRANCH=$TRAVIS_PULL_REQUEST_BRANCH"
  - npm test
  - npm run build
  # - export BRANCH=$(if [ "$TRAVIS_PULL_REQUEST" == "false" ]; then echo $TRAVIS_BRANCH; else echo $TRAVIS_PULL_REQUEST_BRANCH; fi)

after_success:
  - git config --global user.email "praveenguda.kumar@gmail.com"
  - git config --global user.name "gudapraveenkumar"
  - git config --list
  - git checkout $TRAVIS_PULL_REQUEST_BRANCH
  - git branch
  - git pull
  - git checkout $TRAVIS_BRANCH
  - git branch
  - git merge $TRAVIS_PULL_REQUEST_BRANCH
  - git push origin $TRAVIS_BRANCH
